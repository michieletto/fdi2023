// Include the standard input/output library
#include <stdio.h>

// Main function
int main() {
    // Make the cat meow 3 times

    // Make the cat meow
    printf("meow!\n");
    // Make the cat meow
    printf("meow!\n");
    // Make the cat meow
    printf("meow!\n");

    // Pretty simple, right?
    // ...but there should be a better way
    // what if we want to make the cat meow 100 times?
    // or 1000 times?
    // or 1000000 times?
    // or 1000000000 times?
    // It would be a pain to write all those lines of code

    // Return 0 to indicate success
    return 0;
}
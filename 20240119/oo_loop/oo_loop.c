// oo_loop.c
// Include le librerie necessarie
#include "oo_loop.h"         // Libreria con le funzioni

/**
 * Duplica la capacità dell'array di interi
 * @param [in] array puntatore all'array di interi
 * @param [inout] capacity puntatore alla capacità totale dell'array
 * @return puntatore all'array di interi
**/
int *resize_array(int *array, int *capacity) {
    return NULL;
}

/**
 * Legge un array di interi da tastiera fino a quando 
 * non viene inserito un valore negativo
 * @param [out] size puntatore alla dimensione dell'array
 * @param [out] capacity puntatore alla capacità totale dell'array
 * @return puntatore all'array di interi
**/
int *read_array(int *size, int *capacity) {
    return NULL;
}